package uz.frodo.educationsystem.a

//import android.os.Bundle
//import uz.frodo.educationsystem.ARG_PARAM1
//import uz.frodo.educationsystem.ARG_PARAM2
//import uz.frodo.educationsystem.InsideFragment

//    1.Mentorga qara,
//    2.mehrob aka intent orqali
//    3. A.D. type id qimasdan

/*companion object {
    //        var updateData:()->Unit={
//        }
    fun newInstance(param1: String, param2: Int) =
        InsideFragment().apply {
            arguments = Bundle().apply {
                putString(ARG_PARAM1, param1)
                putInt(ARG_PARAM2,param2)
            }
        }
}

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
//        updateData={
//            updateAdapter()
//
//        }
    arguments?.let {
        param1 = it.getString(ARG_PARAM1)
        param2 = it.getInt(ARG_PARAM2)
    }
}


TabLayoutMediator(binding.tab, binding.viewPager) { tab, position ->
            //InsideFragment.updateData.invoke()
            when (position) {
                0 -> {
                    tab.text = "Ochilgan guruxlar"
                }

                1 -> {
                    tab.text = "Ochilayotgan guruxlar"
                }
            }
        }.attach()*/